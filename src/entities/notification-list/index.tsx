import React from 'react';

const NotificationList = () => {
    return (
        <div className={'notification-list'} style={{ display: "none" }}>

        </div>
    );
};

export default NotificationList;